# Genie v0.1.0 产品需求文档 (PRD)

> **项目愿景**: 智能代码生成与自动部署平台 - 用户输入想法，AI生成并部署完整应用

---

## 📋 **项目概述**

### **核心价值主张**

Genie是一个AI驱动的应用生成平台，用户通过自然语言描述需求，系统自动完成代码生成、项目构建和云端部署，并返回可访问的应用链接。

### **目标用户**

- 非技术人员：快速原型验证想法
- 开发者：快速生成MVP和工具脚本
- 产品经理：需求演示和概念验证

### **v0.1.0版本范围**

- ✅ 自然语言输入解析（LangGraph Studio Web UI）
- ✅ 基于Claude Agent SDK的单智能体代码生成
- ✅ 本地代码生成（examples/ai_projects/目录下自动创建项目文件夹）
- ✅ 本地服务验证（Generate节点）
- ✅ E2B沙箱部署（Deploy节点创建沙箱）
- ✅ LangGraph Studio实时进度反馈
- ✅ LangGraph工作流编排
- ✅ **单轮对话模式**：每次执行接收一次用户输入，完成代码生成和部署后结束
- ⏳ 暂不包含：CLI工具、用户账户、历史记录、复杂应用支持、**多轮交互对话**

---

## 🎯 **功能需求**

### **1. 用户输入层**

#### **1.1 输入方式**

- **自然语言描述**（主要方式）
  - 支持中文/英文描述
  - 示例："我想要一个待办事项管理工具，支持添加、删除、标记完成"
  - 示例："Build a calculator app with basic operations"

#### **1.2 需求复杂度支持**（渐进式）

| 版本     | 支持范围          | 示例                       |
| -------- | ----------------- | -------------------------- |
| v0.1.0   | 简单工具/单页应用 | 计算器、待办事项、简单表单 |
| 未来版本 | 中等应用          | 博客系统、CRM、多页面应用  |
| 未来版本 | 复杂系统          | 电商平台、SaaS应用         |

---

### **2. 后端核心能力**

#### **2.1 技术栈**

- **工作流编排**: LangGraph
- **开发界面**: LangGraph Studio（通过 `langgraph dev` 启动）
- **代码生成**: Claude Agent SDK (Python) - 单智能体模式
- **代码生成目录**: examples/ai_projects/{project_folder}/ - 本地文件系统
- **沙箱部署**: E2B Sandbox
- **交互方式**: LangGraph Studio Web UI

#### **2.2 LangGraph工作流设计**

**核心节点**（简化为3节点）:

```python
workflow = StateGraph(AgentState)

# 节点定义
workflow.add_node("initialize", initialize_node)  # 在examples/ai_projects/下创建项目文件夹
workflow.add_node("generate", generate_node)      # 调用Claude Agent SDK生成代码并本地验证
workflow.add_node("deploy", deploy_node)          # 创建E2B沙箱、复制代码、部署服务

# 边定义（线性流程）
workflow.set_entry_point("initialize")
workflow.add_edge("initialize", "generate")
workflow.add_edge("generate", "deploy")
workflow.add_edge("deploy", END)
```

**AgentState结构**:

```python
from typing import TypedDict, Optional, List, Dict, Any

class AgentState(TypedDict):
    # 用户输入
    user_input: str              # 用户输入的需求描述

    # 项目管理
    project_folder: str          # 项目文件夹名称（使用时间戳格式：proj_YYYYMMDD_HHMMSS）
    project_path: str            # 完整项目路径（examples/ai_projects/{project_folder}）

    # Claude Agent SDK会话管理
    claude_sdk_session_id: str   # Claude Agent SDK会话ID（在generate节点初始化）
    messages: List[Dict[str, Any]]  # 完整会话历史（包含所有与Claude主智能体的交互消息）
    conversation_turn: int        # 当前对话轮次计数
    last_assistant_message: Optional[str]  # 最后一条助手消息文本（用于快速显示）

    # 本地验证（generate节点）
    local_validation_passed: bool  # 本地服务验证是否通过
    local_validation_port: int     # 本地验证使用的端口

    # E2B沙箱部署（deploy节点创建）
    e2b_template: str            # E2B模版名称
    sandbox_id: str              # E2B沙箱ID（deploy节点创建）
    sandbox_url: str             # E2B沙箱访问链接
    sandbox_expires_at: str      # 沙箱过期时间（ISO 8601格式）
    deployment_port: int         # 沙箱部署端口号
    access_url: str              # 最终访问链接

    # 执行状态
    error: Optional[str]         # 错误信息
    logs: list                   # 执行日志（LangGraph Studio显示）
```

**Messages字段设计说明**:

`messages`字段用于保存用户与Claude Agent SDK主智能体的完整会话历史，每条消息包含：

```python
{
    "type": str,           # 消息类型: UserMessage, AssistantMessage, ToolUseBlock, ResultMessage等
    "timestamp": str,      # 消息时间戳（ISO 8601格式）
    "content": Dict,       # 消息内容（序列化后的完整消息对象）
    "metadata": Dict       # 可选：额外元数据（如token使用量、成本等）
}
```

**消息收集机制**:
- Generate和Deploy节点在接收到Claude Agent SDK消息时**立即**添加到`messages`列表
- 保持消息顺序和完整性，便于LangGraph Studio展示会话历史
- 支持时间线可视化和消息类型分类显示

---

### **3. Claude Agent SDK集成**

#### **3.1 持续会话模式配置**

使用Claude Agent SDK的 `ClaudeSDKClient` 实现持续会话，确保Generate和Deploy节点共享上下文：

```python
from claude_agent_sdk import ClaudeSDKClient, ClaudeAgentOptions
import os
from datetime import datetime

# ===== Initialize节点：只创建项目文件夹和E2B沙箱 =====
project_folder = f"proj_{datetime.now().strftime('%Y%m%d_%H%M%S')}"
project_path = f"examples/ai_projects/{project_folder}"
os.makedirs(project_path, exist_ok=True)

# ===== Generate节点：初始化SDK会话 =====
# ClaudeAgentOptions配置
options = ClaudeAgentOptions(
    # 工作目录指向新创建的项目文件夹
    cwd=project_path,

    # 工具链配置
    allowed_tools=["Read", "Write", "Edit", "Bash", "Glob", "Grep"],
    permission_mode="bypassPermissions",  # 自动接受文件编辑

    # 系统提示词（使用Claude Code预设）
    system_prompt={
        "type": "preset",
        "preset": "claude_code",
        "append": """你是一个全栈开发专家。根据用户需求，在当前目录生成完整的项目代码。
要求：
1. 生成可直接运行的完整项目
2. 包含所有必要的配置文件（package.json, requirements.txt等）
3. 编写清晰的README说明文档
4. 代码符合最佳实践和规范
5. 项目结构清晰合理"""
    },

    # 流式输出（用于进度反馈）
    include_partial_messages=True
)

# 在Generate节点中创建持续会话客户端
client = ClaudeSDKClient(options)
session_id = client.session_id  # 保存会话ID到AgentState
```

#### **3.2 持续会话工作流**

**Initialize节点**：

1. 创建项目文件夹：`examples/ai_projects/proj_{timestamp}/`
2. 初始化AgentState基础字段
3. 初始化 `messages` 为空列表
4. 设置本地验证端口范围（如3000-3010，8000-8010）

**Generate节点**（关键：在此节点初始化Session ID并验证）：

1. **初始化ClaudeSDKClient会话**并保存 `session_id` 到AgentState
2. 使用ClaudeSDKClient发送代码生成请求
3. Agent在项目文件夹内生成完整代码
4. **流式收集消息**：每收到SDK消息立即添加到 `state["messages"]`
5. **本地验证**：
   - 在端口范围内（3000-3010或8000-8010）尝试启动服务
   - 执行基本健康检查（HTTP GET /或自定义健康端点）
   - 验证前后端服务是否正常响应
   - 记录验证结果到 `local_validation_passed` 和 `local_validation_port`
   - 停止本地服务（为Deploy节点释放资源）
6. 验证生成文件的完整性

**Deploy节点**（关键：创建沙箱并部署）：

1. **创建E2B沙箱**：
   - 根据项目类型选择合适的E2B模版（Node.js、Python等）
   - 创建沙箱实例并保存 `sandbox_id` 和 `sandbox_expires_at`
2. **复制项目代码到沙箱**：
   - 将本地项目文件夹完整复制到沙箱内部
   - 使用E2B SDK的文件上传功能
3. **在沙箱中部署服务**：
   - 从AgentState获取同一个 `session_id`
   - 使用同一ClaudeSDKClient会话连接
   - **继续收集消息**：部署过程中的所有消息追加到 `state["messages"]`
   - 在该会话中运行E2B SDK代码：
     - 安装依赖（npm install / pip install）
     - 启动服务（使用本地验证成功的端口）
     - 执行健康检查
4. 生成访问链接：`https://{sandbox_id}.e2b.dev:{port}`
5. 返回部署状态和访问链接

#### **3.3 消息收集实现示例**

**Generate节点中的消息收集代码**：

```python
from datetime import datetime
from claude_agent_sdk import ClaudeSDKClient, AssistantMessage, UserMessage, ResultMessage

async def generate_node(state: AgentState) -> AgentState:
    # 初始化SDK会话
    client = ClaudeSDKClient(options=options)
    state["claude_sdk_session_id"] = client.session_id
    state["messages"] = []
    state["conversation_turn"] = 0

    # 发送代码生成请求
    await client.query(f"根据需求生成完整项目代码：{state['user_input']}")

    # 流式收集消息
    async for message in client.receive_messages():
        # 立即序列化并添加到messages
        message_data = {
            "type": type(message).__name__,
            "timestamp": datetime.now().isoformat(),
            "content": serialize_message(message),  # 自定义序列化函数
        }

        # 添加元数据
        if isinstance(message, ResultMessage):
            message_data["metadata"] = {
                "cost_usd": message.total_cost_usd,
                "input_tokens": message.usage.input_tokens,
                "output_tokens": message.usage.output_tokens
            }

        state["messages"].append(message_data)

        # 更新最后一条助手消息（用于快速显示）
        if isinstance(message, AssistantMessage):
            for block in message.content:
                if isinstance(block, TextBlock):
                    state["last_assistant_message"] = block.text

        state["conversation_turn"] += 1

    return state
```

#### **3.4 持续会话技术优势**

使用ClaudeSDKClient持续会话模式相比传统方式的优势：

1. **上下文保持**：

   - Generate和Deploy节点共享完整对话历史
   - Deploy节点可直接引用代码生成过程的信息
   - 避免重复说明项目结构和配置
2. **集成部署**：

   - 在同一会话中执行E2B SDK代码
   - Agent理解本地项目结构，可智能部署
   - 减少文件传输和环境配置复杂度
3. **错误恢复**：

   - Deploy失败时，Agent了解完整生成过程
   - 可基于历史信息快速定位问题
   - 便于后续版本实现断点续传
4. **效率提升**：

   - 避免重复环境初始化
   - 减少API调用和会话建立开销
   - 提高端到端执行效率

#### **3.4 代码质量标准**

- **文件完整性**：

  - 包含入口文件（index.html, main.js, app.py等）
  - 包含依赖配置（package.json, requirements.txt等）
  - 包含README.md说明文档
- **代码规范**：

  - 符合语言标准和最佳实践
  - 代码结构清晰，易于理解
  - 包含必要的注释
- **可部署性**：

  - 项目可独立运行
  - 依赖声明完整准确
  - 启动命令明确

---

### **4. E2B沙箱部署**

#### **4.1 集成部署流程**

**Generate节点** - 本地验证：

1. 生成完整项目代码到本地文件夹
2. 在本地端口范围（3000-3010或8000-8010）启动服务
3. 执行健康检查验证服务可用性
4. 记录验证结果和成功端口
5. 停止本地服务

**Deploy节点** - 创建沙箱并部署：

1. **创建E2B沙箱实例**：
   - 根据项目类型选择E2B模版（e2b-nodejs、e2b-python等）
   - 创建沙箱并记录sandbox_id和过期时间
2. **复制代码到沙箱**：
   - 将完整项目文件夹复制到沙箱内部
   - 使用E2B SDK的filesystem API上传文件
3. **在沙箱中部署**：
   - 连接到同一Claude SDK会话（使用 `session_id`）
   - 在该会话中运行E2B SDK代码：
     - 安装依赖（npm install / pip install）
     - 启动服务（使用本地验证成功的端口）
     - 执行健康检查
4. 返回访问链接：`https://{sandbox_id}.e2b.dev:{port}`

#### **4.2 关键功能**

- **E2B模版选择**:

  - Node.js项目：使用 `base` 或 `node` 模版
  - Python项目：使用 `base` 或 `python` 模版
  - 根据 `package.json` 或 `requirements.txt` 自动判断
  - 未来支持更多语言模版
- **本地验证**（Generate节点）:

  - 端口范围：前端3000-3010，后端8000-8010
  - 健康检查：HTTP GET请求验证响应
  - 超时保护：单服务启动最多等待30秒
  - 失败处理：记录错误但不中断流程
- **依赖安装**:

  - 自动检测 `package.json`/`requirements.txt`
  - 执行 `npm install`/`pip install`
- **环境变量处理**:

  - 预设通用环境变量（PORT、NODE_ENV等）
  - 未来支持用户自定义环境变量
- **端口管理**:

  - 使用本地验证成功的端口
  - 支持多服务场景（前端 + 后端）
- **错误处理**:

  - 部署失败自动重试（最多3次）
  - 收集并返回详细错误日志
  - 超时保护（单次部署最长5分钟）

#### **4.3 沙箱限制**

- **执行时间**: v0.1.0暂不限制代码生成和部署时间（未来版本可添加超时控制）
- **资源限制**: 使用E2B默认配置（未来可自定义）
- **沙箱有效期**: 统一设置为1小时
  - 创建沙箱时记录过期时间
  - 在响应中明确提示有效期
  - 过期后沙箱自动销毁
- **错误恢复**: v0.1.0不支持断点续传，失败需完全重新执行

---

### **5. 用户交互体验（LangGraph Studio）**

#### **5.1 启动和访问**

通过LangGraph Studio启动和使用Genie：

```bash
# 启动LangGraph开发服务器
langgraph dev

# 浏览器自动打开 http://localhost:2024
# 访问LangGraph Studio Web UI
```

#### **5.2 Web UI交互流程**

1. **输入需求**：在Studio界面的输入框输入自然语言需求

   - 示例："我想要一个待办事项管理工具"
2. **查看工作流执行**：

   - Studio实时显示工作流节点执行状态
   - 可视化展示 Initialize → Generate → Deploy 流程
3. **实时日志查看**：

   - 查看每个节点的执行日志
   - 观察Agent的文件操作和代码生成过程
   - 查看E2B部署进度

#### **5.3 实时反馈内容**

LangGraph Studio界面显示：

- **初始化阶段**：

  ```
  [Initialize Node]
  ✓ 项目文件夹创建：examples/ai_projects/proj_20250112_143000
  ✓ 本地验证端口范围：前端3000-3010，后端8000-8010
  ```
- **代码生成阶段**：

  ```
  [Generate Node]
  📝 Claude Agent工作目录：examples/ai_projects/proj_20250112_143000
  ✓ 创建 index.html
  ✓ 创建 app.js
  ✓ 创建 style.css
  ✓ 创建 package.json
  ✓ 创建 README.md
  ✅ 代码生成完成！

  🔍 本地验证中...
  ✓ 前端服务启动成功 (端口: 3000)
  ✓ 健康检查通过
  ✓ 本地验证完成，服务已停止
  ```
- **部署阶段**：

  ```
  [Deploy Node]
  🏗️ 创建E2B沙箱 (模版: base)...
  ✓ 沙箱创建成功 (sandbox_abc123)
  ✓ 沙箱有效期：1小时（至 2025-01-12 15:30:00）
  📦 复制项目文件到沙箱...
  🔧 安装依赖 (npm install)...
  🚀 启动服务 (端口: 3000)...
  ✅ 部署完成！
  ```

#### **5.4 结果展示**

工作流完成后，Studio界面显示最终结果：

```
═══════════════════════════════════════
🎉 应用部署成功！
═══════════════════════════════════════

📍 访问链接: https://abc123.e2b.dev:3000
📁 本地代码: examples/ai_projects/proj_20250112_143000/
🔧 SDK路径: examples/ai_projects/proj_20250112_143000/
⏱  沙箱有效期: 1小时（至 2025-01-12 15:30:00）
⏱  总耗时: 8分32秒

提示：
- 代码已保存在本地项目文件夹
- 沙箱将在1小时后自动销毁
- 可通过会话历史查看项目路径和访问链接
- 可在Studio中重新运行工作流
═══════════════════════════════════════
```

---

## 🏗️ **技术架构**

### **系统架构图**（简化版）

```
┌──────────────────────────────┐
│   LangGraph Studio Web UI    │
│   (http://localhost:2024)    │
│   - 输入需求                 │
│   - 查看工作流可视化         │
│   - 实时日志显示             │
└──────────┬───────────────────┘
           │
           ▼
┌────────────────────────────────────────────────┐
│         LangGraph 工作流 (Python)              │
│  ┌──────────────────────────────────────┐     │
│  │  1. Initialize Node                  │     │
│  │  - 创建项目文件夹                    │     │
│  │    examples/ai_projects/proj_xxx/   │     │
│  │  - 初始化AgentState.messages为空    │     │
│  │  - 设置本地验证端口范围              │     │
│  ├──────────────────────────────────────┤     │
│  │  2. Generate Node                    │     │
│  │  - 初始化ClaudeSDKClient会话        │◄────┼─→ Claude Agent SDK
│  │  - 保存session_id到AgentState       │     │   (持续会话)
│  │  - Agent在项目文件夹内生成代码      │     │   [Read/Write/Edit/Bash]
│  │  - 流式收集消息到messages列表       │     │
│  │  - 本地验证：启动服务+健康检查      │     │
│  │  - 记录验证结果，停止服务            │     │
│  ├──────────────────────────────────────┤     │
│  │  3. Deploy Node                      │     │
│  │  - 创建E2B沙箱（有效期1小时）       │◄────┼─→ E2B Sandbox API
│  │  - 复制项目代码到沙箱                │     │
│  │  - 使用同一session_id连接           │◄────┼─→ Claude Agent SDK
│  │  - 继续收集消息到messages列表       │     │   + E2B SDK
│  │  - 在沙箱中部署服务并返回链接        │     │
│  └──────────────────────────────────────┘     │
└────────────────────────────────────────────────┘
           │
           ▼
┌──────────────────────────────┐
│   Studio界面显示结果         │
│   - 访问链接                 │
│   - 本地代码路径             │
│   - 执行日志                 │
│   - 会话历史(messages)       │
└──────────────────────────────┘
```

### **核心模块**（简化）

#### **Module 1: Workflow Engine** (`src/workflow/`)

- `graph.py` - LangGraph图定义（3节点）
- `state.py` - AgentState结构
- `nodes/` - 工作流节点实现
  - `initialize.py` - 初始化节点（创建项目文件夹）
  - `generate.py` - 代码生成节点（调用Claude Agent SDK + 本地验证）
  - `deploy.py` - 部署节点（创建沙箱 + 复制代码 + 部署）

#### **Module 2: Agent Manager** (`src/agents/`)

- `agent.py` - ClaudeSDKClient持续会话管理
- `prompts.py` - 系统提示词模板
- `session.py` - 会话生命周期管理

#### **Module 3: Deployment** (`src/deployment/`)

- `e2b_client.py` - E2B SDK封装
- `remote_deployer.py` - 通过Claude Agent SDK远程部署逻辑
- `deployment_script.py` - E2B沙箱内执行的部署脚本

#### **Module 4: Utilities** (`src/utils/`)

- `logger.py` - 日志系统（控制台输出）
- `validators.py` - 输入验证
- `exceptions.py` - 自定义异常
- `file_ops.py` - 文件操作工具（清理工作目录等）

---

## 🔒 **非功能需求**

### **1. 性能**

- **执行时间**: v0.1.0暂不设置强制时间限制
  - 建议目标：代码生成 <15分钟，部署 <5分钟
  - 超时处理留待后续版本实现
- **并发支持**: v0.1.0单进程运行（未来支持多进程）

### **2. 安全**

- **输入验证**: 基本长度和格式检查
- **E2B沙箱隔离**: 生成的代码在隔离环境运行
- **沙箱生命周期**: 统一1小时有效期，自动销毁

### **3. 可维护性**

- 代码覆盖率: >70%
- 文档完整性: 关键功能有文档说明
- 日志完整性: 关键操作有控制台日志

### **4. 可扩展性**

- 可配置工作流图（支持新节点）
- 可扩展Agent提示词模板
- 支持新的部署目标（未来）

---

## 📦 **交付物**

### **v0.1.0 Deliverables**

- ✅ LangGraph工作流实现（3节点）
- ✅ Claude Agent SDK单智能体集成
- ✅ E2B沙箱部署功能
- ✅ examples/ai_projects/ 代码生成目录（自动创建项目文件夹）
- ✅ LangGraph Studio配置
- ✅ README和快速开始指南
- ✅ 使用示例和文档

---

## 🚀 **开发路线图**

### **Phase 1: 核心能力**

- [X] 需求探索和文档编写 ← 当前阶段
- [ ] LangGraph工作流实现（3节点）
- [ ] 初始化节点：E2B沙箱创建 + 项目文件夹生成
- [ ] 生成节点：Claude Agent SDK集成
- [ ] 部署节点：E2B代码上传和部署

### **Phase 2: LangGraph Studio集成**

- [ ] LangGraph Studio配置
- [ ] 工作流可视化展示
- [ ] 实时日志集成
- [ ] 状态管理优化

### **Phase 3: E2B部署优化**

- [ ] 代码上传流程优化
- [ ] 端口管理和链接生成
- [ ] 依赖安装自动化
- [ ] 错误处理和重试机制

### **Phase 4: 测试与优化**

- [ ] 单元测试和集成测试
- [ ] 性能优化（15分钟生成时间）
- [ ] 文档完善（Studio使用指南）
- [ ] v0.1.0发布

---

## 📊 **成功指标**

### **功能完整性**

- [ ] 能成功生成并部署简单HTML应用
- [ ] 能成功生成并部署React应用
- [ ] 能处理至少80%的简单应用需求

### **质量指标**

- [ ] 代码生成成功率 >85%
- [ ] 部署成功率 >90%
- [ ] 建议完成时间目标：端到端 <20分钟（不强制限制）

### **用户体验**

- [ ] 提供清晰的实时进度反馈
- [ ] 错误信息可理解且可操作
- [ ] 生成的代码质量达到可用标准

---

## ✅ **已决策问题汇总**

### **技术决策**

1. ✅ **项目文件夹命名**: 使用时间戳格式 `proj_YYYYMMDD_HHMMSS`

   - 简单直观，自动按时间排序
   - 避免UUID的可读性问题
2. ✅ **Claude Agent SDK模式**: 使用ClaudeSDKClient持续会话

   - Generate和Deploy节点共享同一会话上下文
   - Deploy节点通过SDK会话运行E2B SDK代码
   - 实现集成化的远程部署流程
   - 提高效率，便于错误恢复

3. ✅ **Session ID初始化**: 在Generate节点初始化

   - Initialize节点只创建项目文件夹
   - Generate节点创建ClaudeSDKClient并保存session_id
   - 避免Initialize节点中不必要的SDK初始化开销

4. ✅ **E2B沙箱创建时机**: 在Deploy节点创建

   - Initialize节点不创建沙箱，专注于本地文件夹初始化
   - Deploy节点根据E2B模版创建沙箱
   - 先生成代码再创建沙箱，优化资源使用
   - 便于根据项目类型选择合适的E2B模版

5. ✅ **本地验证**: Generate节点添加服务验证

   - 生成代码后在本地端口范围启动服务
   - 执行健康检查验证服务可用性
   - 验证成功后停止服务，记录端口信息
   - 为Deploy节点提供验证过的端口配置

6. ✅ **会话历史管理**: AgentState.messages保存完整会话历史

   - 每个节点收到SDK消息立即添加到messages列表
   - 包含消息类型、时间戳、内容和元数据
   - 支持LangGraph Studio可视化展示
   - 便于调试和会话历史回顾

7. ✅ **单轮对话模式**: v0.1.0只支持单次用户输入

   - 每次工作流执行接收一次用户需求描述
   - 完成代码生成和部署后工作流结束
   - 不支持在生成过程中的多轮交互和需求澄清
   - 未来版本将支持多轮对话和增量修改

8. ✅ **超时处理**: v0.1.0暂不限制执行时间

   - 降低开发复杂度，聚焦核心功能
   - 后续版本可添加软限制（警告）和硬限制（终止）

9. ✅ **错误恢复**: v0.1.0不支持断点续传

   - 简化实现，失败需完全重新执行
   - 持续会话设计为未来断点续传奠定基础
   - 建议记录关键步骤日志

10. ✅ **项目管理**: 不在Studio中列出历史项目

    - 用户通过LangGraph Studio会话历史获取
    - 避免重复开发项目管理UI
    - AgentState中保存完整项目信息供查询

### **产品决策**

1. ✅ **使用频率**: v0.1.0暂不限制

   - 快速验证产品价值，收集使用数据
   - 后续版本根据数据制定合理限制
   - 建议添加简单使用日志记录
2. ✅ **沙箱有效期**: 统一设置为1小时

   - 平衡资源成本和用户体验
   - 创建时返回过期时间（ISO 8601格式）
   - 在响应中清晰提示有效期
3. ✅ **历史项目访问**: 支持通过会话历史和AgentState获取

   - AgentState包含 `project_path`、`sandbox_url`、`sandbox_expires_at`、`claude_sdk_session_id`
   - 响应消息清晰展示项目路径和访问信息
   - Claude Agent SDK使用持续会话模式
4. ✅ **项目清理**: 完全手动清理

   - v0.1.0不实现自动清理机制
   - 用户可手动删除 `examples/ai_projects/` 下的历史项目
   - 简化实现，聚焦核心功能
5. ✅ **错误日志**: 保留所有诊断信息

   - Agent执行完整日志
   - E2B部署错误详情
   - 系统错误堆栈
   - 便于调试和问题追踪

---

## 🤔 **待决策问题**

目前v0.1.0所有核心决策已完成，可以开始技术实现。

### **未来版本考虑**

1. **多轮交互对话**: 支持在代码生成过程中与用户进行多轮对话
   - 需求澄清：Agent主动提问不明确的需求
   - 增量修改：基于用户反馈迭代改进代码
   - 人机协作：允许用户在生成过程中介入和调整

2. **超时控制机制**: 考虑添加软限制（警告）和硬限制（终止）

3. **断点续传**: 复杂项目生成中断后的恢复机制

4. **自动清理**: 历史项目的自动清理策略

5. **并发支持**: 多进程/多线程处理并发请求

6. **使用配额**: 基于使用数据的合理限制策略

7. **E2B模版扩展**: 支持更多语言和框架的E2B模版

8. **本地验证增强**: 支持更复杂的验证场景（数据库、API等）

---

## 📚 **参考资源**

- [Claude Agent SDK文档](https://github.com/anthropics/claude-agent-sdk-python)
- [LangGraph官方文档](https://python.langchain.com/docs/langgraph)
- [E2B Sandbox文档](https://e2b.dev/docs)

---

**文档版本**: v1.0
**状态**: ✅ 需求探索完成，待技术实现
